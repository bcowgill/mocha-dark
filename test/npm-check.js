// This indicates to the npm-check (make depends) command that these modules
// are actually used, just not used by require()
require('prettier') // from Makefile
require('requirejs') // from mocha-test.html etc
